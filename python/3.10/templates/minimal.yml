# =============================================================================
# template: minimal  —  a bare, fast-solving starting point
# -----------------------------------------------------------------------------
# Fork this for a new project when you want a clean slate. Rename `py310-min`
# and add only what the project actually imports.
# =============================================================================
name: py310-min
channels:
  - conda-forge
dependencies:
  - python=3.10.*
  - ipykernel             # so the env shows up as a Jupyter kernel
  - pip
  - rich                  # nicer tracebacks/logging out of the box
